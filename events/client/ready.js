module.exports = async (Discord, client) => {
    //console.log("Diprox is online!")
    //const delay = (msec) => new Promise((resolve) => setTimeout(resolve, msec));

    //await delay(4000);
    console.log('\x1b[31m', `Logged in as ${client.user.username}. Ready on ${client.guilds.cache.size} servers, for a total of ${client.users.cache.size} users`)
}